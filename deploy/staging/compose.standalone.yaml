services:
  edge:
    environment:
      EDGE_PROXY_PROTOCOL: ""
      EDGE_CLIENT_ADDRESS: $$remote_addr
      EDGE_FORWARDED_FOR: $$proxy_add_x_forwarded_for
    ports:
      - "80:8080"
      - "443:8443"

  edge-bootstrap:
    ports:
      - "80:8080"
